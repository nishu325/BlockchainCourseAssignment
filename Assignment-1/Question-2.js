
// ask the value
const cel = prompt("Enter a value in celsius : ");

// calculate data
const fahren = (cel * 1.8) + 32

// display final result
console.log(`${cel} degree celsius is equal to ${fahrenh} degree fahrenheit.`);